# Overlay Studio · 安装指南

从零到导出第一个透明 MOV。全程复制粘贴，不需要懂代码。

**Windows 和 macOS 都适用。** 大部分步骤两边一模一样，只有三处不同（装 Node、装
ffmpeg、设镜像的写法），这三处我会分开写，**照你自己的系统看那一段就行**。

预计 Windows 40–60 分钟、Mac 20–30 分钟，大部分时间在等下载。

---

## 开始之前：两件事先确认

**1. 放项目的那个盘，至少留 20GB 空闲。**
导出是先出 PNG 序列再合成视频，空间吃得很凶：1 分钟成片约 4.5GB、3 分钟约 9.6GB、
4 分半约 13GB。空间不够会得到一个半截的 MOV，剪映只会说「文件损坏」，看不出真正原因。

**关键是「放项目的那个盘」——不是 C 盘。** 导出的中间帧和成片都写在项目目录的
`exports/` 里，项目在 D 盘，它们就在 D 盘。所以 Windows 用户如果 C 盘紧张，
把项目放 D 盘就解决了大半。

C 盘会被占掉的只有这些，大约 1–1.5GB，躲不掉：Node.js 本体（约 100MB）、
导出用的 Chrome（约 540MB，装 puppeteer 时自动下的）、ffmpeg（约 150MB）、
npm 下载缓存（几百 MB）。实在紧张可以搬走，见文末「C 盘实在不够怎么办」。

**2. 项目要放在纯英文路径下。**

- **Windows**：建议放**空间大的那个盘**，比如 `D:\overlay-studio`（C 盘够用也行）
- **macOS**：建议放 `~/overlay-studio`

**路径里不要有中文、不要有空格**（Windows 别放桌面和「我的文档」，Mac 别放「下载」里
带中文的文件夹）。视频处理工具对中文路径的兼容性一直不好，这一条能省掉后面一半的怪问题。

> 下文命令里写的都是 Windows 路径。Mac 用户把 `D:\overlay-studio` 换成
> `~/overlay-studio`、反斜杠 `\` 换成斜杠 `/` 即可。

**3. 这套工具不附带字体和音效文件，需要你自己准备。**

不是遗漏，是授权问题：中文字体和音效的授权条款各不相同，很多标着「免费商用」的素材并
**不允许「随软件再分发」**——「你能用」和「我能转给你」是两回事。所以这两类文件必须由
使用者自己获取。

| 缺什么 | 不装的后果 | 怎么取 | 放哪 |
|---|---|---|---|
| **中文字体** | 动效退回系统默认字体，**排版和粗细跟预览不一样**，大标题最明显 | 见第 6 步（推荐思源黑体，开源可商用） | `src\assets\fonts\` |
| **音效** | 导出时自动跳过配音效，画面完全正常 | 自己买或用免费音效库，`.mp3` 格式 | `public\sfx\` |

**字体建议装，音效可以先不管。** 字体直接影响成片长相，音效只是锦上添花。

---

## 第 1 步：装 Node.js

### Windows

打开 <https://nodejs.org/>，下载 **LTS 版**（左边那个绿色按钮），双击 `.msi` 安装，一路下一步。

装完**关掉所有已打开的终端窗口**，重新开一个 PowerShell：

> 开始菜单搜索「PowerShell」→ 打开 **Windows PowerShell**

### macOS

装了 [Homebrew](https://brew.sh/) 的话最省事，打开「终端」：

```bash
brew install node
```

没装 Homebrew 就去 <https://nodejs.org/> 下 **LTS 版** 的 `.pkg`，双击安装。

### 两个系统都要验证

```
node -v
```

看到 **`v22.12` 或更高**就对了（`v24`、`v25` 都行）。导出用的 puppeteer 要求 22.12 以上，
Node 20 装依赖时会报 `EBADENGINE`——遇到就回上面重装一次 LTS 版。

提示「不是内部或外部命令」/ `command not found`，说明安装没生效——**先关掉终端重开**，
还不行就重启电脑。

---

## 第 2 步：装 ffmpeg（Windows 最容易卡住的一步）

导出透明视频靠它。**它不会跟着项目自动装，得单独装**——两个系统都要。

### macOS

一行搞定：

```bash
brew install ffmpeg
```

装完跑下面两条，都能看到版本信息就行，**跳到第 3 步**：

```bash
ffmpeg -version
ffprobe -version
```

（没装 Homebrew 的话，先在 <https://brew.sh/> 按首页那行命令装它，再回来。）

### Windows · 先试自动安装

```powershell
winget install --id Gyan.FFmpeg -e
```

装完**关掉 PowerShell 重新打开**（很重要，不重开读不到新配置），然后验证：

```powershell
ffmpeg -version
ffprobe -version
```

两条都看到一大段版本信息就成功了，跳到第 3 步。

### Windows · 如果 winget 用不了，手动装

（提示 `winget 不是内部或外部命令` 说明系统版本太老，走这条）

1. 打开 <https://www.gyan.dev/ffmpeg/builds/>，下载 **ffmpeg-release-essentials.zip**
2. 解压，把里面那个文件夹改名成 `ffmpeg`，整个移到 `C:\ffmpeg`
3. 确认 `C:\ffmpeg\bin\ffmpeg.exe` 这个文件存在（路径不对后面全白搭）
4. 把它加进系统 PATH：
   - 开始菜单搜索「环境变量」→ 打开**编辑系统环境变量**
   - 点右下角**环境变量**按钮
   - 在**下方**「系统变量」里找到 `Path`，选中，点**编辑**
   - 点**新建**，填 `C:\ffmpeg\bin`
   - 一路点**确定**（三个窗口都要点确定，中途关掉不生效）
5. **关掉 PowerShell 重新打开**，再跑 `ffmpeg -version` 和 `ffprobe -version` 验证

> 90% 的失败是这两个原因：`bin` 忘了写，或者没重开终端。

---

## 第 3 步：拿到代码

这一步在浏览器里做，之后的步骤都可以让 Claude Code 帮你跑。

1. 浏览器打开 <https://github.com/jeszhou/overlay-studio>
2. 点绿色的 **Code** 按钮 → **Download ZIP**
3. 解压，把解压出来的文件夹改名成 `overlay-studio`，移到：
   - **Windows**：`D:\overlay-studio`（或任何空间充足的盘，C 盘够用也行）
   - **macOS**：`~/overlay-studio`

> **路径里不要有中文和空格**。Windows 别放在桌面或「我的文档」里，
> 解压出来的文件夹名如果带一串乱码或 `-main` 后缀，改掉就行。

<details>
<summary>想以后能一键更新？用 GitHub Desktop 克隆（可选）</summary>

下载 ZIP 最省事，但以后有更新要重新下一次。想一键更新的话改用这个：

1. 装 <https://desktop.github.com/>，用你的 GitHub 账号登录
2. 菜单 **File → Clone repository → GitHub.com**
3. 列表里选 `overlay-studio`
4. **Local path 改成上面那个纯英文路径**（默认路径可能带中文用户名）
5. 点 **Clone**

以后有更新，打开 GitHub Desktop 点一下 **Fetch origin** 就行。

改过源码、或者当初是下载 ZIP 装的，更新方式不一样 —— 见项目根目录的
**《更新指南.md》**，里面有可以直接复制给 AI 助手的提示词。

</details>

## 第 4 步：安装依赖

PowerShell 里依次跑（**一行一行来，别一次全粘**）：

```powershell
cd D:\overlay-studio\motion-playground
```

```powershell
npm config set registry https://registry.npmmirror.com
```

第三行两个系统写法不同，**照自己的系统抄**：

```powershell
# Windows（PowerShell）
$env:PUPPETEER_DOWNLOAD_BASE_URL="https://cdn.npmmirror.com/binaries/chrome-for-testing"
```

```bash
# macOS（终端）
export PUPPETEER_DOWNLOAD_BASE_URL="https://cdn.npmmirror.com/binaries/chrome-for-testing"
```

```powershell
npm install
```

中间两行是换成国内镜像。第三行尤其重要——这一步要下载一个几百兆的浏览器内核，不换镜像
大概率卡住不动。

`npm install` 要跑 3–10 分钟，中间刷一堆字是正常的。看到最后一行显示 `added xxx packages`
就成功了。

> **注意**：第三行的设置只在当前这个终端窗口有效。中途关了窗口要重装的话，
> 三行都得重新跑一遍。

### 确认 AI Skill 也完整

仍在 `motion-playground` 目录里运行：

```powershell
npm run check:agent-skills
```

正常会看到：

```text
4 个 Agent 入口一致
```

Overlay Studio 会把同一份 `overlay-fx-generator` Skill 分别放到 Claude Code、Codex、
CodeBuddy / WorkBuddy 能识别的项目目录。使用 AI 编排时，请让 Agent 打开**整个
`overlay-studio` 仓库根目录**，不要只打开 `motion-playground`。

如果检查提示缺少 `.claude`、`.agents`、`.codebuddy` 或 `.workbuddy`，说明代码副本不完整，
先重新下载/更新完整仓库，不要继续生成编排。

> **⚠️ 成片质量跟你用的模型有关。**
>
> 卡片库、导出管线、体检器是固定的，谁用都一样。但「这段话该配哪张卡、章节怎么分」
> 是 AI 判断出来的——**模型强，段落切得准、卡选得贴；模型弱，跑得起来但糙一档**。
>
> 所以编排不满意的时候，先换个更强的模型再试一遍，别急着怀疑工具。
>
> 另外：这个 Skill 要求 AI **能读写你电脑上的文件、也能执行命令**（交付前要跑一次
> 编排体检）。纯网页版的聊天 AI 做不到这两件事——它能给你一份 JSON，但不会自检，
> 你可能导进去才发现时间对不上。请用带本地文件权限的 AI 编程工具。

---

## 第 5 步：启动，看到界面

```powershell
npm run dev
```

看到 `Local: http://localhost:5177/` 就成了。**这个窗口不要关**——关掉服务就停了。

> **这不是一个网站。** `localhost` 的意思就是「你这台电脑本身」，`5177` 是个门牌号。
> 软件是在你自己电脑上跑的，浏览器只是拿它当窗口用——断网照样能开，别人也打不开你这个地址。
> 想换号码就用 `npm run dev -- --port 5188`，或者改 `motion-playground/vite.config.ts` 里的 `port`。

浏览器打开 <http://localhost:5177>，点顶栏**「🎬 示例」**，会载入一套 28 秒的演示编排，
按空格播放。

**能播动画 = 装好了。** 剩下两步是让导出成品达到应有的效果。

---

## 第 6 步：装字体（不装的话成片长相会不对）

**仓库不附带字体文件。** 中文字体授权条款各不相同，很多写着「免费商用」的字体并不允许
「随软件再分发」——这是两回事。所以字体要自己下载。

不装字体，动效卡会退回系统默认字体，**排版和粗细都会跟预览效果不一样**，大标题尤其明显。

**推荐装思源黑体**（Adobe 开源，SIL OFL 许可，可自由使用和再分发，最省心）：

1. 打开 <https://github.com/adobe-fonts/source-han-sans/releases>
2. 在最新版本下找到 **Assets** 列表——**默认是折叠的，要先点「Show all 21 assets」才看得全**。
   下载 **`19_SourceHanSansCN.zip`**（简体中文子集版）。

   > 注意文件名**前面带 `19_` 编号**，直接搜「SourceHanSans」会跳出 21 个结果。
   > 列表里还有个长得很像的 `09_SourceHanSansSC.zip`，那是全字符集版，
   > 体积大一倍，本工具的预览效果基于 **CN** 版，认准编号别拿错。

3. 解压。**字体不在最外层** —— 要点进 **`SubsetOTF`** → **`CN`** 两层文件夹，
   里面才是 7 个字重。**只需要其中 3 个**，复制到
   `D:\overlay-studio\motion-playground\src\assets\fonts\`
4. **按下表改名**（名字必须一模一样，包括中文和短横线）：

| 解压出来的文件 | 改名成 |
| --- | --- |
| `SourceHanSansCN-Regular.otf` | `思源黑体-400.otf` |
| `SourceHanSansCN-Bold.otf` | `思源黑体-700.otf` |
| `SourceHanSansCN-Heavy.otf` | `思源黑体-900.otf` |

剩下 4 个字重（ExtraLight / Light / Normal / Medium）用不上，不用复制。

改完刷新浏览器，左栏「全局字体」下拉里就会出现「思源黑体」。

> **为什么一定要按「家族名-字重数字」改名**：这样三个字重会合并成一个下拉项，各字重按真实
> 字形渲染。不这么写，浏览器只能拿唯一那个字重去「伪粗」，大标题会糊成一团。干净的重黑体
> 效果靠的是真字重，不是伪粗。

---

## 第 7 步：导出，验证全链路

在 Studio 里点**「⬇ 导出透明 MOV」**。

第一次导出建议**先拿示例编排试**，别上来就用自己的长视频——链路通不通，10 秒的片子就能验出来。

成品在 `D:\overlay-studio\motion-playground\exports\output\`。

拖进剪映**置顶轨道**，盖在原片上，不需要做任何对位或转换。

> **关于帧率**：导出固定是 **29.97fps**（也就是 NTSC，手机和相机拍的视频绝大多数是这个）。
> 素材如果是 30 / 25 / 60fps 也照样能用——剪映是按**时间**对齐轨道的，不是按帧数，
> 长度一致就不会越走越偏，顶多动效偶尔重复一帧，肉眼看不出来。

**到这里全部装完了。**

---

## 报错对照表

| 现象 | 原因 | 怎么办 |
|---|---|---|
| `node 不是内部或外部命令` / `node: command not found` | Node 没装上或没重开终端 | 关终端重开；再不行重启电脑 |
| `ffmpeg 不是内部或外部命令` / `ffmpeg: command not found` | PATH 没配好，或没重开终端 | 关终端重开。Windows 再不行就回第 2 步检查 `C:\ffmpeg\bin\ffmpeg.exe` 存不存在；Mac 重跑 `brew install ffmpeg` |
| 导出中断，说「需要 ffprobe(用来读视频时长)」 | ffmpeg 和 ffprobe 分开装了，只装上一个 | 回第 2 步重装一次（winget / brew 会两个一起装上），装完重开终端 |
| 导出中断，说「渲染器(Chromium)没装好」 | 第 4 步装依赖时浏览器内核没下下来（多半是当时网断了或没设镜像） | 在项目文件夹里跑 `npx puppeteer browsers install chrome`，跑完重新导出 |
| `npm install` 卡在 puppeteer 不动 | 没设镜像，在连国外服务器 | Ctrl+C 中断，把第 4 步三行重跑一遍 |
| `npm run dev` 报端口被占用 | 5177 被别的程序占了 | 换一个：`npm run dev -- --port 5188`，浏览器地址也跟着改 |
| Mac 提示「无法验证开发者」 | 系统拦了下载的安装包 | 系统设置 → 隐私与安全性 → 点「仍要打开」 |
| 浏览器打开是空白 | 服务没在跑 | 回 PowerShell 看窗口是不是关了，重跑 `npm run dev` |
| 导出的 MOV 剪映说「文件损坏」 | **多半是磁盘写满了** | 清出 20GB 以上空间重新导出 |
| 成品字体跟预览不一样 | 字体没装或命名不对 | 回第 6 步，检查文件名是不是「家族名-字重数字」格式 |
| 导出没有音效 | 仓库不附带音效文件 | 正常现象，音效需自备，放进 `public\sfx\` |

---

## C 盘实在不够怎么办（Windows）

先确认一件事：**项目本身放 D 盘就行**，导出吃掉的那十几 GB 会跟着项目走，不占 C 盘。
下面说的是那 1–1.5GB 赖在 C 盘的部分，只有 C 盘特别紧张才需要动。

在 PowerShell 里执行，**要在 `npm install` 之前**：

```powershell
npm config set cache D:\npm-cache --global
setx PUPPETEER_CACHE_DIR D:\puppeteer-cache
```

第二条设完**必须关掉 PowerShell 重新打开**才生效（`setx` 改的是系统环境变量，
当前窗口读不到）。

如果你已经装过了：改完重跑一次 `npm install`，Chrome 会重新下到 D 盘，
原来的 `C:\Users\你的用户名\.cache\puppeteer` 可以手动删掉，能回收约 540MB。

---

## 遇到没列出的问题

把这三样发给我，我这边能直接定位：

1. **终端里报错的完整内容**（Windows 在 PowerShell 里右键即可复制）
2. 你卡在**第几步**
3. 一张截图
