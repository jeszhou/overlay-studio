# 更新日志

每一版发生了什么变化，最新的在最上面。

**版本号怎么读**（`主版本.次版本.补丁`）：

| 哪一位涨了 | 意味着 | 你要做什么 |
| --- | --- | --- |
| 补丁位　1.0.0 → 1.0.**1** | 修 bug | 直接更新，零风险 |
| 次　位　1.0.1 → 1.**1**.0 | 加了新功能 | 直接更新，你原来的做法照常能用 |
| 主　位　1.1.0 → **2**.0.0 | 有破坏性变更 | **更新前先看这一节** —— 你已有的编排或习惯用法可能需要调整 |

界面左上角的徽章显示你正在用哪一版，鼠标停上去能看到档位和发布日期。提 issue 时带上这个号，能省掉一轮来回。

---

## v1.1.1 — 2026-09-04

源码整理,没有功能变化。编辑台的用法和 v1.1.0 完全一样,直接更新即可。

### 变更

- 整理了一遍源码,结构更干净;给 AI 读的说明书随之精简。你已有的编排和习惯用法不受影响。

## v1.1.0 — 2026-09-04

多了一个「版本指南」入口,和 README 底部的作者信息。编辑台本身的用法没有任何变化。

### 新增

- **左上角的版本徽章可以点了**,打开一份版本指南:两版分别能做什么、差在哪、以及怎么找到作者。
- **效果库列表末尾多了一个入口**,同样通向那份指南 —— 翻完所有卡还没找到想要的表达时,那儿会告诉你还有别的选择。搜索状态下不出现。
- README 最下面多了「关于作者」一节,带抖音和小红书二维码。

### 变更

- 两档对外的叫法统一成**基础版 / 专业版**。你手上这份是基础版,徽章上也这么写。

---

## v1.0.1 — 2026-09-02

四个会伤到成片的 bug，加上说明书里几处和实际对不上的地方。直接更新，零风险。

**你导出的成片、导入的素材、各期的编排文件，一个都不会动。**

### 修复

- **换了口播视频再导出，运镜卡里烤进去的还是上一条。** 预览里看着是新视频，导出的 MOV 里是旧的，而且没有任何提示。现在换视频时全局「口播视频」跟着换（你在侧栏手动选过别的口播视频的话，那个选择不动）。
- **拖错一个编排文件进窗口，正在做的编排直接被替换。** 现在会先问一句，和「载入示例」「清空」一样；⌘Z 照样能撤。
- **导出卡死一次之后，再点导出永远是「已有导出任务在进行中」，只能重启。** 现在渲染器 30 秒没动静就报错退出；15 分钟没有任何进展会强制停掉并解锁。
- **长口播 + 运镜卡，导出前说磁盘「够」，导到一半盘满了。** 卡内视频抽帧占的空间以前没算进预检，现在算了；导出成功后会清掉这次没用到的抽帧缓存；渲染到一半失败时，半成品 PNG 目录也会清掉，不再默默占几个 G。
- 刷新页面恢复上次编排时，如果里面有不认识的卡，现在会跳过它们，而不是整页打不开（导入时早就这样，恢复这条路以前漏了）。
- 导入编排跳过了不认识的卡时，提示会说明：再导出 JSON 这几张不会带回去，原文件留好。

### 变更（只是说明书）

- 操作指南第一行不再写死一个版本号 —— 你在用哪一版看左上角徽章。
- 使用指南：Node 要求改成和安装指南一致的 22.12；删卡改成「时间轴上选中按 Delete」；字体设置指向顶栏 ⚙；「卡片怎么选」那张表按现有的 20 张卡重写 —— 之前那张表和实际对不上。
- 操作指南里几处和实际对不上的做法，已改成能做的做法。
- README 的用途分组数改成 11（之前写 12）。
- 演示说明里的「示例」按钮位置改成顶栏。

## v1.0.0 — 2026-09-02

版本号从这一版起正式启用。

### 变更

- **左上角的版本徽章开始显示真实版本号。** 此前它一直写着 `v7.15` —— 那是初版留下的一段固定文字，和实际版本没有关系，从来没有更新过。所以你看到数字从 7.15 变成 1.0.0 **不是降级**，是这个位置第一次说真话。
- **从这一版起，每次更新都会在这里留一条记录**，同时在仓库的 Releases 页面发一次。

### 说明

1.0.0 之前的改动没有逐条补记 —— 那时还没有版本号，补记出来的分界线是假的。想看这之前的历史，翻仓库的提交记录即可。

这一版的功能范围已经定下来了：一条能独立跑通的完整生产线，从导入口播视频到导出透明动效层。后续更新以修复为主。
