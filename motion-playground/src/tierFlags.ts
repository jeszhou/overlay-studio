/**
 * 发行版功能开关。
 *
 * 完整版全开;免费版关掉几项(每一项的注释里写了为什么)。
 *
 * 关掉的都是"省事"那一层 —— 关了照样能完整做出一期能发的视频,只是慢一点、手动一点。
 * 分层的尺子在这儿:能不能做完 = 公开;做得多快多省事 = 付费。
 * 所以时间轴、画布、参数面板、体检器、导入导出、撤销重做这些一个都不在这张表上。
 */
export const TIER = {
  /** 参数预设:把一张卡调好的全部参数存成可复用组合,下次一键套上 */
  presets: false,
  /** 换卡时把文案/条目/节奏/落位搬到新卡(关掉后换卡给的是空模板,内容要重填) */
  kindSwapCarry: false,
  /** 效果库里单独导出一张卡的透明片段(做素材/样片用;整条时间轴导出不受影响) */
  singleCardExport: false,
  /** 「玻璃底色」控件(卡片自带的一块垫底,亮画面上保证字看得清)。
   *  ⚠️ 关掉之前想清楚:免费版没有 frost-screen / glass-pane 这类常驻垫层卡,
   *  纯文字/纯数字卡(blur-text / odometer / ring-metric / stat-proof / type-shift)
   *  在亮画面上就没有任何补救手段了。这是"能不能做完一期"的边界,不是装饰。 */
  glassControl: false,
  /** 「投影」控件(把卡从画面里托出来)。纯装饰,没有任何卡默认非 none,关掉零风险 */
  shadowControl: false,
  /** 竖版画幅(9:16):侧栏「画幅」开关 + 特效卡在场时人像让位。
   *  关掉只是收起开关,横版一条口播照样从头做到尾 —— 竖版是"多一种画幅",不是"能不能做完"。
   *  ⚠️ 收进这张表的原因:竖版的代码散在 App.tsx / Sidebar.tsx 这些公共文件里,
   *  不用开关管着,就没有任何东西拦得住它整份出现在免费版里。 */
  verticalRatio: false,
} as const;
