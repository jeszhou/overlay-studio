内置演示(Studio 左栏「🎬 示例」按钮)只需要这个文件夹里的两个文件:
  demo-overlay.json  —— 28s / 10 张卡的示例编排
  demo.srt           —— 配套字幕稿

要在 `focus-card` 的图槽里放自己的录屏,把 mp4/webm/mov 丢进这个
文件夹,然后在 Studio 右栏填 /demo/文件名.mp4,
文件会被一起渲染进透明动效层。

注意:本文件夹除上述两个文件外的内容不进仓库(见根目录 .gitignore),
你自己放的素材只在本地生效,不会被误提交。
