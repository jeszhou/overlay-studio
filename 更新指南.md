# 更新指南

以后有更新，照这份做就行。

---

## 先看一眼：这次要不要小心

编辑台**左上角的徽章**显示你正在用哪一版（鼠标停上去能看到发布日期）。
项目根目录的 **`CHANGELOG.md`** 写清楚每一版变了什么，最新的在最上面。

版本号三位数各代表什么：

| 哪一位涨了 | 意味着 | 你要做什么 |
| --- | --- | --- |
| 补丁位　1.0.0 → 1.0.**1** | 修 bug | 直接更新，零风险 |
| 次　位　1.0.1 → 1.**1**.0 | 加了新功能 | 直接更新，你原来的做法照常能用 |
| 主　位　1.1.0 → **2**.0.0 | 有破坏性变更 | **先看 CHANGELOG 那一节**，你已有的编排可能要调整 |

想知道有没有新版本：在仓库页面右上角点 **Watch → All Activity**，有新版就会收到邮件。

---

## 你是哪种安装？

打开终端，进到项目目录（里面有个叫 `motion-playground` 的文件夹），跑：

```bash
git rev-parse --is-inside-work-tree
```

- 输出 `true` → **克隆装的**，看下面【一】。以后每次更新点两下就完事。
- 报错说不是 git 仓库 → **下载 ZIP 装的**，看【二】。做一次迁移，以后就轻松了。

---

## 一、克隆装的：怎么更新

### 情况 A：你没改过软件源码（绝大多数人）

**GitHub Desktop**：点 `Fetch origin`，再点 `Pull origin`。完事。

**命令行**：

```bash
git pull
```

### 情况 B：你改过源码

先把自己的改动存住，再拉：

```bash
git add -A && git commit -m "我自己的修改"
git pull
```

**出现冲突就停下来，别自己挑一边。** 冲突不是出错，是 git 不知道该保谁的。
把附录那段提示词发给你的 AI 助手，它会把每处冲突讲清楚再让你选。
你第一步已经 commit 存过一份，选错也退得回来。

### 拉完之后

1. `package.json` 有变化 → 进 `motion-playground` 跑 `npm install`（没变化就跳过）
2. 跑 `npx tsc -b` 确认没有编译错误
3. 启动：进 `motion-playground` 跑 `npm run dev`

第一次打开可能会重新弹一次协议页 —— 协议内容一改就会重弹，滚到底点同意即可。

### 遇到这两个报错，停下来问我

- `divergent branches`
- `unrelated histories`

**不要自己找变通办法**，尤其不要跑 `git pull --rebase`、不要 `git reset`。
把原话贴到仓库的 Issues 里，我看到会回。

---

## 二、ZIP 装的：迁移一次，以后就不用再来

思路：**新的放到旁边，再把你的东西搬过去。旧目录全程不动**，它是你的退路。

### 第 1 步：克隆一份新的（这一步你本人做）

1. 装 [GitHub Desktop](https://desktop.github.com/)，用你的 GitHub 账号登录
2. 菜单 `File → Clone repository → URL`，填 `https://github.com/jeszhou/overlay-studio`
   （或者 `File → Clone repository → GitHub.com`，先在网页上点个 Star / Fork 让它出现在列表里）
3. 列表里选 `overlay-studio`
4. **Local path 改成纯英文路径，放在旧目录旁边、名字和旧的不一样**
   （默认路径常带中文用户名，一定要改）
   - Windows：`D:\overlay-studio-new`
   - Mac：`~/overlay-studio-new`
5. 点 Clone

### 第 2 步：把你的东西搬过去

**全部用复制，不要用移动。** 旧目录里有就搬，没有就跳过：

| 搬什么 | 是什么 |
| --- | --- |
| `motion-playground/exports/` | 你导出的成片和帧序列（可能很大） |
| `motion-playground/public/_media/` | 你导入的口播视频 |
| `motion-playground/src/assets/fonts/` | 你自己装的中文字体（别覆盖新目录里的 README.md） |
| `motion-playground/public/sfx/` | 你自己的音效（同样别覆盖 README.md） |
| `motion-playground/public/logos/` 里的 `.png` | 你补进去的品牌图标（`.svg` 是软件自带的，不用管） |
| `motion-playground/public/demo/` 里除 `demo-overlay.json`、`demo.srt`、`README.txt` 之外的 | 你自己放的素材 |
| 四个 skill 目录里的《我的偏好.md》《经验规则.md》 | 你调教出来的口味。带 `.default` 的是自带模板，**别用它覆盖你的** |
| 「9月01日」这种日期名的文件夹 | 你各期的工作目录 |
| 根目录所有 `overlay` 开头的 `.json` | 你的编排文件 |
| 任何叫 `STATUS.md` 的文件 | 连同它所在位置一起对应过去 |

**搬完一定要再对一遍。** 上面这张表是按常见情况列的，很可能漏掉你随手放的东西。
附录那段提示词里有扫描命令，会把「只有旧目录有、新目录没有」的全部列出来让你逐项确认。

### 第 3 步：跑起来

```bash
cd 新目录/motion-playground
npm install
npx tsc -b
npm run dev
```

### 第 4 步：确认没问题之前，旧目录一个字都别动

- 先在新版本里打开以前的编排，确认内容都在
- 再确认一遍：成片（exports）、素材（_media）、字体、各期工作目录都在
- 都正常了，你自己再决定要不要把旧目录改名留着当备份

**以后再有更新，GitHub Desktop 点一下 Fetch origin 就行了**，不用再走这一整套。

---

## 最高优先级：宁可更新失败，也不要删东西

**这条比把命令跑通更重要。** 如果你让 AI 助手帮忙，把这段一起给它：

- **不要删除任何文件或文件夹。** 不要 `rm`、不要 `rm -rf`、不要移动、不要"清理一下"。
  哪怕看起来是没用的缓存或临时文件，也先停下来确认。这条没有例外。
- **绝对不要跑 `git clean`。** `exports` 里是你导出好的成片，`public/_media` 里是你导入的视频，
  这两个在 git 管辖之外，`git clean` 会连它们一起删，删了找不回来。
- **不要 `git reset --hard`、`git checkout .`、`git restore`**，这几个会把你改过的代码打回原样。
- 迁移时旧目录只读不写：只许复制出来，不许往里写、不许改名、不许删。
- 不要为了让命令跑通而绕路。失败就把报错原样贴到仓库 Issues。
- 拿不准就停下来，别猜。

---

## 附录：可以整段复制给 AI 助手的提示词

改过源码、或者要从 ZIP 迁移时，把下面这一整段复制给你的大模型（Claude / ChatGPT / Codex 都行）。
**这段是通用的，每次更新都能用。**

```
帮我更新 Overlay Studio 这个项目。

这件事做成的标准只有一条：**新版本跑起来了，而我原来的东西一样不少**
（成片、导入的素材、字体、各期编排、我自己改过的代码）。
两者冲突时，保我的东西优先——更新可以下次再来，我的东西丢了找不回来。

请按顺序做，每步做完告诉我结果。我不懂技术，请用大白话跟我说话，不要甩术语。

项目根目录有一份《更新指南.md》，请你先完整读一遍，按里面的步骤做。
另外根目录还有 CHANGELOG.md，读一下最上面那一版，把这次的变化用大白话讲给我。

几条硬规矩，比把命令跑通更重要：
- 不要删除任何文件或文件夹，不要 rm、不要移动、不要"清理"。这条没有例外。
- 绝对不要跑 git clean —— exports 和 public/_media 在 git 管辖之外，删了找不回来。
- 不要 git reset --hard、git checkout . 、git restore。
- 出现冲突就停下来，把每处冲突用大白话讲给我（我原来改成什么、新版改成什么、
  各自什么效果），让我选，我选了你再动手。不要替我挑一边。
- 报错是 divergent branches 或 unrelated histories 的话，停下来把原话告诉我，
  不要自己找变通办法，尤其不要 git pull --rebase、不要 reset。
- 拿不准就停下来问我，不要猜。

如果我是从 ZIP 迁移，搬完文件后请务必做这一步兜底扫描，把「只有旧目录有、
新目录没有」的内容一项一项念给我，不要替我筛：

  Mac / Linux：
    cd "旧目录" && find . -name node_modules -prune -o -name .git -prune \
      -o -name dist -prune -o -print | sort > /tmp/old.txt
    cd "新目录" && find . -name node_modules -prune -o -name .git -prune \
      -o -name dist -prune -o -print | sort > /tmp/new.txt
    comm -23 /tmp/old.txt /tmp/new.txt

  Windows PowerShell：
    $old = Get-ChildItem -Recurse "旧目录" -Exclude node_modules,.git,dist |
           ForEach-Object { $_.FullName.Replace("旧目录","") }
    $new = Get-ChildItem -Recurse "新目录" -Exclude node_modules,.git,dist |
           ForEach-Object { $_.FullName.Replace("新目录","") }
    Compare-Object $old $new | Where-Object SideIndicator -eq "<=" |
           Select-Object -ExpandProperty InputObject

另外：不管我说没说改过源码，都先把旧目录的 motion-playground/src 整个复制一份
到新目录旁边，命名「我的旧src-备份」。有了它，我的魔改怎么都丢不了。
```
